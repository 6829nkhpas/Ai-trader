# OpenTofu / Terraform — never commit state or real variable values
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Real variable values & backend secrets (contain OCIDs / paths / keys)
terraform.tfvars
*.auto.tfvars
backend.hcl

# Plans can embed secrets
*.tfplan
