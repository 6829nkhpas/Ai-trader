# deep-quant-loop tests

Property-based and unit tests for the Python LangGraph service.

Property tests use [Hypothesis](https://hypothesis.readthedocs.io/) with `max_examples=100`.

Run with:

```bash
pytest agents/deep-quant-loop/tests
```
